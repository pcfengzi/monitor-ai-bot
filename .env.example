# .env.example - 模板文件（不要将真实密钥提交到仓库）
# 复制此文件为 `.env` 并填入具体值。

# 用于临时覆盖配置文件中的模式
MONITOR_AI_PLUGIN_MODE=dev

#（未来扩展）
MONITOR_AI_DB_URL=sqlite://database/monitor_ai.db

# 填写你的 API Key（不要在真实仓库提交）
MONITOR_AI_API_KEY=


# AI 插件会读取这些
AI_BACKEND=python          # python | openai | deepseek
API_SERVER_BASE=http://127.0.0.1:3001
AI_ENGINE_BASE=http://127.0.0.1:8000

# 如果以后用 OpenAI / DeepSeek，可以再加（在本例中留空）:
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com


# 示例账户凭证（替换为你自己的测试账号，不要提交真实凭证）
USER=your-username@example.com
PASS=your-password
EXPECTED_USER_ID=xx      # 对应响应里的 user.id = xx
API_MONITOR_CONFIG=workflows/api-monitor.toml

# 说明：
# - 不要把真实的私密值（API keys、密码、证书等）提交到仓库。
# - 把本文件复制为 `.env` 并填入有效的本地/测试值供开发使用。
